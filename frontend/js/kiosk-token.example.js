/**
 * Copia este archivo como kiosk-token.local.js (no subir a git) en la misma carpeta.
 * El valor debe coincidir con la variable de entorno KIOSK_TOKEN del servidor.
 */
window.__RHM_KIOSK_TOKEN__ = 'reemplaza-con-tu-secreto-kiosk';
